server:
  port: 8083

spring:
  application:
    name: order service

  datasource:
    url: jdbc:postgresql://localhost:5432/order_db
    username: postgres
    password: bruce

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
